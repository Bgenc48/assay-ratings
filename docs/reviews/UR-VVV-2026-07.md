# UR review memo — VVV (Venice Token)

| | |
|---|---|
| Token | VVV — Venice Token |
| Chain / address | base — `0xacfe6019ed1a7dc6f7b508c02d1b04ec88cc21bf` |
| Computed grade | **D** (overall 45.9, methodology v0.1.0) |
| Published letter | **UR** (Under Review) |
| Liquidity | $14,056,224 |
| Scan reviewed | 2026-07-11T21:25:37Z (`data/tokens/base-0xacfe6019ed1a7dc6f7b508c02d1b04ec88cc21bf.json`) |
| Registry location | `registry/discovered.json` (auto-discovery, 2026-07-11) — any review flag requires promotion to `registry/tokens.json` first |
| Memo prepared | 2026-07-13 |

**Bottom line:** likely a genuine low grade. Every plausible way of
resolving the one unknown (who controls the owner address) recomputes to D
or F — see the robustness table. Run the two live checks below; if they
land where expected, promote VVV to `registry/tokens.json` with
`reviewedLowGrade: true`.

## Why the methodology computed this grade

Weights (no governance claim, standard profile): Supply 20 · Admin 20 ·
Disclosure 15 · Insider 15 · Liquidity 15 · Track 10 · Holders 5. Average
over scorable dimensions; overall = min(average, hard caps).

| Dimension | Score | Weight | Contribution | Driver (from the report) |
|---|---|---|---|---|
| Supply Integrity | 35 | 20 | 700 | A `mint(address,uint256)` path exists; its controller could not be classified (insufficient data) |
| Admin Surface | 40 | 20 | 800 | Owner `0x321b7ff7…f340Ff` classified "unknown" |
| Disclosure Integrity | 70 | 15 | 1050 | No machine-checkable claims (claim-light floor) |
| Insider Float | N/R | — | — | No data; coverage only |
| Liquidity Permanence | 25 | 15 | 375 | Dominant pool (`0x01784e…`, $8.39M, Aerodrome) fully analyzed: LP 0% burned / 0% locked. This is a real reading, not the unanalyzed-pool defect |
| Track Record | 75 | 10 | 750 | 534 days (~17.5 months) |
| Holder Concentration | N/R | — | — | No data; coverage only |
| Governance Reality | N/A | 0 | — | Not claimed |

Weighted average: 3,675 / 80 = **45.94 → 45.9** → D band (40–54.9). Caps
triggered (C unclassified-mint, A age-24mo, B+ claim-light) sit above the
average and do not bind. D above $1M liquidity gates to UR.

A note on "unknown": in this pipeline "unknown" covers two cases the
report cannot distinguish — the probe of the owner address failed at scan
time (a temporary RPC problem), or the owner is a contract that is neither
a Safe multisig nor a timelock. The live check resolves which.

## Robustness: the D does not hinge on the unknown

Recomputing under each possible resolution of the owner classification:

| If the owner turns out to be… | Supply | Admin | Recomputed overall | Letter |
|---|---|---|---|---|
| A Safe multisig (mint becomes "multisig, no timelock") | 35 | 55 | (700+1100+1050+375+750)/80 = **49.7** | D |
| A plain wallet (EOA) — mint controllable by one key | 0 (+ hard cap F) | 25 | (0+500+1050+375+750)/80 = **33.4** | F |
| A contract that is not a Safe or timelock | 35 | 40 | **45.9** (unchanged) | D |

Every branch is D or F. (The task brief for this review stated ≈52.8 for
the multisig branch; that figure assumes a 24-month track record at 100 —
the report shows 534 days → track 75, giving 49.7. The conclusion — still
D — is the same either way.)

## What was independently verified during this review

- The report's arithmetic (above) reproduces exactly from the published
  dimension scores and v0.1.0 weights.
- The liquidity reading is not an artifact of the unanalyzed-pool defect
  fixed in this PR: the dominant VVV pool has `lockAnalyzed: true` with
  0% burned and 0% locked — an actual on-chain LP-distribution read.
- This sandbox has no chain access, so the owner-address classification
  itself could not be re-run here; that is the live check below.

## What still needs a live check

- [ ] **Classify the owner address.** Open
  `https://base.blockscout.com/address/0x321b7ff75154472B18EDb199033fF4D116F340Ff`.
  If the page shows a "Contract" tab (the address has code), the mint gate
  is a contract; if it is a plain address with no code, the mint gate is a
  single wallet key. This is the difference between D and F above.
- [ ] **If it is a contract, check whether it is a Safe.** Blockscout
  usually names Safe proxies on the address page; otherwise use the
  "Read Contract" tab and try `getOwners()` / `getThreshold()`. Readable
  owners and a threshold prove that minting requires multiple signatures.
- [ ] **Check venice.ai's token docs for emissions/vesting statements**
  (`https://venice.ai/token`). VVV has an emissions program; if the docs
  state the mint schedule or controller, those are candidate claim records
  for the extractor (quote + source URL) — claims material, not a direct
  scoring input.
- [ ] **Rescan** (also worth doing because a transient probe failure would
  self-resolve and reclassify the owner automatically):

  ```bash
  # Throwaway clone — for reading results only. A live --only run rewrites
  # data/index.json down to this one token and appends to data/history/,
  # so never commit or push from this clone; delete the folder when done.
  git clone https://github.com/Bgenc48/assay-ratings.git assay-review
  cd assay-review
  npm install --ignore-scripts
  npm run scan -- --only 0xacfe6019ed1a7dc6f7b508c02d1b04ec88cc21bf
  # then read: data/tokens/base-0xacfe6019ed1a7dc6f7b508c02d1b04ec88cc21bf.json
  ```

## Recommendation

If the live checks land in any of the three rows of the robustness table
(they must — the rows are exhaustive), the computed grade is genuine.
Confirming it requires two steps because VVV currently lives in
`registry/discovered.json`, which is machine-written; review flags belong
on hand-curated entries only.

**Promote to `registry/tokens.json`** (add to the `tokens` array), with the
official source of the address cited in the PR description:

```json
{
  "chain": "base",
  "address": "0xacfe6019ed1a7dc6f7b508c02d1b04ec88cc21bf",
  "expectSymbol": "VVV",
  "name": "Venice Token",
  "reviewedLowGrade": true
}
```

> PR description must cite the official source for this address —
> TODO(owner): confirm it on Venice's own page (venice.ai/token) and link
> it. The symbol gate protects against typos, but the citation is policy.

If instead the rescan reclassifies the owner as a Safe, the letter is
still D (49.7) — the same promotion entry applies; just re-read the fresh
report first so the memo trail matches what publishes. If anything else
unexpected appears, leave VVV at UR and write a follow-up memo.

## Interactions with this PR

- The unanalyzed-pool defect fix does **not** change VVV (dominant pool was
  genuinely analyzed). No number in this memo moves when the PR merges.
- The v0.2 category profiles (stablecoin/wrapped/native) do not apply to
  VVV; it is a standard project token.
- Promotion + `reviewedLowGrade` should be its own small PR after the live
  checks — do not fold it into this changeset, so the review evidence and
  the flag land together.
